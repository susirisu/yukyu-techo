rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 本人（ログイン中のuid）だけが自分のデータを読み書きできる
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
